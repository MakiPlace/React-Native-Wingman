import { StyleSheet } from "react-native";

export default StyleSheet.create({
  wrapper: {
    flex: 1,
    alignItems: "center",
    backgroundColor: "#eee",
    justifyContent: "center"
  },
  Item: {
    justifyContent: "center",
    alignItems: "center",
    marginTop: "20%"
  }
});
