import { StyleSheet } from "react-native";

export default StyleSheet.create({
  wrapper: {
    flex: 1,
    alignItems: "center",
    backgroundColor: "#eee"
  },
  Item: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center"
  }
});
