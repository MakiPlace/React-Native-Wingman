import { StyleSheet } from "react-native";

export default StyleSheet.create({
  wrapper: {
    flex: 1,
    padding: 16
  },
  title: {
    marginTop: 10,
    marginBottom: 8,
    fontWeight: "500"
  }
});
